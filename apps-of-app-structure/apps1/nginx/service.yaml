apiVersion: v1
kind: Service
metadata:
  name: nginx1
  namespace: nginx
  labels:
    app: nginx1
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: nginx1
